#texture to dds conversion - this feature is also available via IWTE button, select input directory, select output directory

<task_id>            texture_to_dds_directory                           
<directory_in>      "I:\test_dds_conversion" 
<directory_out>     "I:\andback"
<directory_out_increment>     yes                       #yes or no whether to increment the directory name (default is yes to prevent accidental overwriting) 
